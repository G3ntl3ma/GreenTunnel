{
  "name": "@g3ntl3ma/green-tunnel-gui",
  "version": "2.0.7",
  "description": "bypasses DPI (Deep Packet Inspection) systems found in many ISPs (Internet Service Providers)",
  "homepage": "https://github.com/G3ntl3ma/GreenTunnel",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "electron .",
    "generate-icons": "electron-icon-maker --input=./icon.png --output=.",
    "package-mac": "electron-packager . green-tunnel --overwrite --asar --platform=darwin --arch=x64 --icon=./icons/mac/icon.icns --prune=true --out=release-builds --ignore=release-builds",
    "package-win": "electron-packager . green-tunnel --overwrite --asar --platform=win32 --arch=x64 --icon=./icons/win/icon.ico --prune=true --out=release-builds --ignore=release-builds",
    "package-linux": "electron-packager . green-tunnel --overwrite --asar --platform=linux --arch=x64 --icon=./icons/png/1024x1024.png --prune=true --out=release-builds --executable-name=green-tunnel-gui --ignore=release-builds",
    "linux-installer": "electron-installer-debian --name green-tunnel --src release-builds/green-tunnel-linux-x64/ --arch amd64 --config installers/linux/debian.json",
    "windows-installer": "node installers/windows/createinstaller.js"
  },
  "author": "Luca Eisen (https://github.com/G3ntl3ma/)",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^19.0.5",
    "electron": "^40.0.0",
    "electron-icon-maker": "0.0.4",
    "electron-winstaller": "^5.4.0"
  },
  "dependencies": {
    "@g3ntl3ma/green-tunnel": "file:..",
    "electron-window-state": "^5.0.3",
    "jquery": "^4.0.0"
  }
}
